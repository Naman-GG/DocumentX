# Base URL of the DocumentX server (HTTP, for AI requests).
VITE_SERVER_URL=http://localhost:3001

# WebSocket URL of the DocumentX server (Yjs sync + WebRTC signaling).
VITE_WS_URL=ws://localhost:3001
